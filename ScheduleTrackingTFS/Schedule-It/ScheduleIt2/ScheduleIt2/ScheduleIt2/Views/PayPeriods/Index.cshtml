@model IEnumerable<ScheduleIt2.Models.PayPeriod>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PayPeriodLength)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DaysUntilPayPeriod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PayPeriodLength)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DaysUntilPayPeriod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PayPeriodId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PayPeriodId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PayPeriodId })
        </td>
    </tr>
}

</table>
